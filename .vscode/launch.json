{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ts-node",
            "type": "node",
            "request": "launch",
            "args": [
                "${file}"
            ],
            "runtimeArgs": [
                "-r",
            "${workspaceFolder}/src/engine/node_modules/ts-node/register"
            ],
            "cwd": "${workspaceFolder}/src/engine",
           // "protocol": "inspector",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}